<% unless resource.errors.messages.any? %>
  location.reload();
<% else %>
  Overlay.show(
      "<%= escape_javascript render 'form', remote: true %>",
      "<%= t('devise.passwords.forgot') %>"
  );
<% end %>
