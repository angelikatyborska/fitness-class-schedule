<% if Rails.application.routes.recognize_path(request.referrer)[:controller].split('/').first == 'admin' %>
  window.location = "<%= escape_javascript root_path %>";
<% else %>
  location.reload();
<% end %>