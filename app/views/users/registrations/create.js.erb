<% if resource.persisted? %>
  location.reload();
<% else %>
  Overlay.show(
      "<%= escape_javascript render template: 'devise/registrations/new' %>",
      "<%= t('user.signup') %>"
  );
<% end %>
