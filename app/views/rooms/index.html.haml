%h2
  = "#{t('room.all_rooms')}:"
= render 'schedule_items/schedule_table', schedule_items: schedule_items

= link_to 'Prev week', rooms_path(week: week - 1.week)
= link_to 'Next week', rooms_path(week: week + 1.week)