= simple_form_for [:admin, room, room_photo] do |f|
  .form-inputs
    .row
      .medium-12.columns
        = f.input :photo, label: false

  .form-actions
    .row
      .medium-12.columns
        = f.button :button, t('shared.save'), class: 'button action save'